[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
# base_url is resolved per environment in config/settings.py (TEST_ENV).
markers =
    api: tests that call an HTTP API
    ui: tests that use a browser
    smoke: fast checks for critical functionality
    regression: broader functional coverage
